import 'package:flutter/material.dart';

class RedeemTrackerScreen extends StatelessWidget {
  const RedeemTrackerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Redeem tracker"),);

  }
}
