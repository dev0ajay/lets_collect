import 'package:flutter/material.dart';

class PointTrackerScreen extends StatelessWidget {
  const PointTrackerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Point Tracker"),);

  }
}
